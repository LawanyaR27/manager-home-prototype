# Manager Home prototype

Splice-styled manager invite and onboarding, stitched to the existing Day 0 manager home.

```sh
npm start
```

Open `http://localhost:4173/`. Use `123456` for the sign-in code. Confirming the team opens the Day 0 home; **Replay onboarding** returns to the invite.
